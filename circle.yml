general:
  branches:
    only:
      - master
      - production
deployment:
  staging:
    branch: master
    commands:
      - gulp publish
      - netlify deploy --path ./site --site-id datasd --access-token $NETLIFY_TOKEN
